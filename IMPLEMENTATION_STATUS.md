# 구현 현황

기획서(V2) 섹션 19 규칙에 따라 M0부터 순차 진행. 마일스톤 번호는 기획서 섹션 17 기준.

## 완료

### M0 — 프로젝트 뼈대
- FastAPI 앱(`app/main.py`), `POST /telegram/webhook` 엔트리포인트, `GET /health`.
- Postgres 연결 계층(`app/db.py`) — 최초 asyncpg로 시도했으나 이 환경(Python 3.14, Windows, 컴파일러 없음)에서 wheel 빌드가 불가능해 **psycopg3(+psycopg-binary)로 교체**. 순수 async 드라이버라는 기획서 요건(Supabase/Postgres 연결)은 동일하게 충족.
- Gemini API 연동 래퍼(`app/ai/gemini_client.py`) — 키 없으면 기능 비활성화, 서버는 정상 기동.
- 환경변수 기반 설정(`app/config.py`), 모든 값이 없어도 서버가 죽지 않고 경고만 남기도록 구현(키를 아직 발급받지 않은 현재 상태를 고려).
- **완료조건 검증**: DB/BOT_TOKEN/GEMINI 키가 전혀 없는 상태에서도 서버 기동 확인, `/telegram/webhook`에 가짜 업데이트 POST 시 정상 응답(200) 확인.

### M1 — 승인/권한 시스템
- `users.approved`, `users.role` 컬럼 (`migrations/0001_init_users.sql`).
- 미승인 사용자는 `/start` 외 어떤 메시지에도 승인 대기 안내만 받음.
- 관리자(`ADMIN_TELEGRAM_ID`)는 최초 접속 시 자동 승인 + `role=admin` 처리.
- 관리자 명령어 `/승인 <telegram_id>`, `/대기목록` 구현.
- 신규 사용자가 `/start` 하면 관리자에게 승인 대기 알림 전송.

### M2 — 나이입력 & 모드라우팅
- 승인된 사용자가 `learning_mode`가 없으면 나이 입력을 요청하고, 숫자 입력 시 `app/modes.py`의 규칙(10세 이하→CHILD_BEGINNER, 11세→CHILD_BRIDGE, 12세 이상→GENERAL)으로 자동 배정.
- `users.age`, `users.learning_mode` 컬럼.

### M3 — 레벨 진단 (V0)
- 단어 5문항 + 문법 5문항 객관식 세트(`app/placement/questions.py`) — 기획서는 20/10/지문1을 명시하지만, 콘텐츠 대량 저작은 코드 로직과 무관한 별도 작업이라 **로직 검증용으로 문항 수를 축소**(정확한 산출 로직은 문항 수와 무관하게 동일하게 동작).
- 인라인 키보드 기반 객관식 응답 처리 구조를 신규 도입: `app/telegram_client.py`에 `build_inline_keyboard`/`answer_callback_query` 추가, `app/handlers/router.py`에 `callback_query` 업데이트 라우팅 추가. 이 구조는 이후 단어(M5)/문법(M6)/학교시험 예상문제(M13) 객관식에도 그대로 재사용 가능(섹션15-6 "인라인 키보드: 객관식 전반" 요건).
- 정답 채점은 AI 호출 없이 DB(정답 인덱스) 비교로 처리 — 섹션16 "AI 호출 최소화 원칙"과 일치.
- 진단 세션은 별도 테이블 없이 프로세스 메모리(`app/placement/service.py`)로 관리 — 소수 사용자·비고가용성 요구(섹션3-2)를 고려한 축소 설계.
- 결과는 `placement_test_results`에 저장, `users.placement_level`에도 반영. GENERAL/CHILD_BRIDGE 모드 배정 직후 자동 시작(확인질문 없이, 섹션7 학습자동화 원칙), 완료 후 재응시는 `/레벨진단` 명령으로 가능.
- **완료조건 검증**: `tests/test_placement_flow.py` — 전문항 정답(advanced), 전문항 오답(beginner), 존재하지 않는 문항 응답 무시 케이스를 콜백쿼리 업데이트로 시뮬레이션해 통과 확인.

**검증 방법**: 실제 Supabase/Telegram 계정이 아직 없어 `tests/`에서 DB/Telegram 발송을 모킹해 라우터·진단 로직을 직접 테스트 (`pytest` 7건 통과) — 승인 전 차단, 승인 후 나이 질문, 10/11/12세 경계값 모드 배정, 레벨진단 전체 플로우(정답/오답/잘못된 응답)를 검증.

### M4 — 콘텐츠뱅크 사전생성
- `words`, `grammar_questions` 테이블 신규 설계(`migrations/0003_content_bank.sql`) — V1 문서의 정확한 필드가 없어(11-3) 학습에 필요한 최소 필드로 직접 설계: 단어(뜻/품사/발음기호/예문/예문해석/레벨), 문법(레벨/주제/문제/4지선다/정답인덱스/해설).
- Gemini에 `response_mime_type: application/json`으로 구조화된 응답을 요청해 레벨별(beginner/intermediate/advanced) 단어·문법 문항을 대량 생성 (`app/content/generator.py`) — 섹션16 "콘텐츠뱅크는 최초 1회만 AI 호출, 이후 캐싱 재사용" 원칙에 따라 상시 API가 아닌 1회성 스크립트(`scripts/generate_content_bank.py`)로 구현.
- 응답 검증: 필수 필드 누락, 선택지 개수(4개 아님), 정답 인덱스 범위 밖 등 형식이 어긋난 항목은 자동 필터링 후 경고 로그만 남김(전체 배치를 실패시키지 않음).
- 단어는 `word` UNIQUE 제약으로 중복 자동 스킵(`on conflict do nothing`).
- **완료조건 검증(실제 Gemini + Supabase로 실행)**: `python scripts/generate_content_bank.py --words-per-level 15 --grammar-per-level 10` 실행 → 단어 53개, 문법 39개 실제 DB 적재 확인 (일부 중복 단어는 정상적으로 스킵됨). 생성된 샘플 내용도 육안 검수함(문법적으로 올바른 문장, 정답 인덱스 정확).
- 순수 로직(JSON 파싱/검증)은 `tests/test_content_generator.py`에서 Gemini 호출을 모킹해 검증 (정상 항목 통과, 필드 누락/선택지 개수 오류/정답 인덱스 범위 오류 필터링, 배열이 아닌 응답에 대한 예외).

### M5 — 단어학습 + SRS + 단어시험
- `user_words` 테이블(`migrations/0004_user_words.sql`) — 사용자별 단어 SRS 상태(status/ease/interval_days/next_review_date), `users.daily_new_word_limit`(기본 5) 추가.
- SRS 계산은 섹션14 공식 그대로 순수 함수로 구현(`app/srs.py`, AI 호출 없음): 정답 시 `interval *= ease`(ease 유지), 오답 시 1일로 리셋 + ease -0.2(최소 1.3).
- 단어 학습(`/단어학습`, 📚 단어 학습): 오늘 복습 대상(간격반복 기준, `next_review_date<=오늘`) + 신규 단어(레벨별, 하루 한도만큼)를 합쳐 카드로 제시 → [아는단어]/[모르는단어] → 모르는 단어만 객관식(뜻 고르기)→주관식(뜻 직접 입력) 순으로 복습, 확인질문 없이 자동 진행(섹션7).
- 단어시험(`/단어시험`, 🔤 단어 시험): 이미 학습한 단어 중 샘플 5개를 객관식으로만 재시험, SRS에는 영향 없음, 반복 응시 가능 — 섹션3-1/16과 일치.
- 하루 신규 단어 수 자동 조절(`app/srs.adjust_daily_new_word_limit`): 세션 정답률 90% 이상이면 +1(최대15), 50% 미만이면 -1(최소3) — 기준은 기획서에 없어 임의로 정함.
- 주관식 채점: AI 없이 문자열 유사도 비교(`app/vocab/quiz.is_meaning_match`) — 초기엔 단순 부분일치였으나, **실제 텔레그램 테스트 중 "모호하게 만들다"가 정답("모호하게 하다, 애매하게 만들다")인데 오답 처리되는 버그를 발견**해 `difflib.SequenceMatcher` 기반 유사도 비교로 개선. 재현 케이스를 `tests/test_vocab_quiz.py`에 고정.
- **완료조건 검증**: `tests/test_vocab_flow.py`(아는단어 SRS 갱신, 모르는단어→객관식→주관식 정답/오답 시 interval·ease 값 검증, 단어시험 전체 플로우)로 로직 검증. 이어서 실제 Supabase+Gemini 콘텐츠로 실제 텔레그램 봇을 통해 12개 단어 전체 플로우(아는단어/모르는단어/객관식/주관식 혼합)를 끝까지 진행해 DB(`user_words`) 반영을 확인.

### 메뉴 UX 개선 + 인라인 키보드 메시지 정리 (사용자 실사용 피드백 반영)
실사용 테스트 중 사용자가 두 가지를 요청함: (1) 슬래시 명령어 대신 버튼 기반 메인 메뉴, (2) 이미 답한 인라인 키보드 카드가 채팅에 계속 남아 헷갈리는 문제.
- `app/menu.py`: 리플라이 키보드 기반 메인 메뉴(🗓오늘의학습/📚단어학습/🔤단어시험/✍️문법학습/📖해석/💬회화/📄텍스트붙여넣기/🔁복습/📊진도확인 + 관리자에게만 ⚙️관리자) 신규 추가. `/메뉴` 명령 또는 버튼으로 언제든 재호출, 학습 세션(레벨진단/단어학습/단어시험) 종료 시 자동으로 다시 노출.
- 버튼 라벨 클릭은 기존 슬래시 명령어와 동일한 핸들러로 라우팅(예: "📚 단어 학습" == `/단어학습`). 아직 구현되지 않은 카테고리(오늘의학습/해석/회화/텍스트붙여넣기/진도확인)는 "다음 마일스톤에서 연결됩니다" 안내로 처리(M6에서 문법학습/복습은 구현 완료).
- `app/telegram_client.py`에 `delete_message`/`build_reply_keyboard` 추가. `_handle_callback_query`에서 콜백을 받으면 **원본 메시지(버튼이 달린 카드/문제)를 항상 삭제**하도록 중앙화 — 레벨진단/단어카드/단어MCQ/단어시험 전부에 동일하게 적용됨. DB에 저장된 학습 기록(정답 여부, SRS 상태 등)은 이 삭제와 무관하게 그대로 유지.
- 사용자에게 확인한 결과, 채점 피드백 메시지("정답입니다!" 등)와 사용자가 직접 입력한 답 텍스트는 **삭제하지 않기로 결정** — 버튼이 없는 일반 대화 메시지라 "이미 눌렀는지" 헷갈리는 문제가 없고, 대화 흐름이 자연스럽게 남는 것을 선호함.
- 중복/지연된 인라인 버튼 클릭(예: 폴링 재시작 중 발생한 stale callback)에 안전하도록 `_handle_vocab_unknown`에 현재 스테이지가 "card"일 때만 처리하는 가드 추가.
- **완료조건 검증**: `tests/test_menu.py` (메뉴 명령/버튼 라우팅, 미구현 placeholder, 콜백 시 메시지 삭제 호출 확인) + 실제 텔레그램에서 메뉴 버튼 클릭 및 카드 삭제 동작 확인.

### M6 — 문법 학습 + 오답 기록
- `user_grammar_answers` 테이블(`migrations/0005_user_grammar_answers.sql`) — 정답여부/오답유형태그/복습여부/응시시각.
- 문법 학습(`/문법학습`, ✍️ 문법 학습): 사용자 레벨의 문항 중 5개를 무작위로 뽑아 객관식으로 순서대로 제시, 확인질문 없이 자동 진행. 정답이면 바로 다음 문제, 오답이면 정답+해설을 보여준 뒤 다음 문제 (콘텐츠뱅크의 `explanation` 필드 재사용, 추가 AI 호출 없음 — 섹션16 원칙과 일치).
- **하루 1회 제한**: 신규 세트(`/문법학습`)는 하루 1번만 가능(섹션3-1 "문법/해석/회화는 신규 세트 하루 1회"), 이미 했으면 안내 메시지와 함께 `/복습`을 권유.
- `/복습`(🔁 복습): 신규 세트 여부와 무관하게 언제든 무제한 반복 가능. 현재는 문법만 지원(해석/회화가 아직 없어 자연스럽게 문법 전용으로 축소 — 추후 M8/M9 구현 시 확장 가능).
- 오답 유형 태그: 기획서 섹션18 [결정필요] 항목("오답 유형 태그의 구체적 종류/이름")에 대해, 별도 태그 체계를 새로 만드는 대신 **M4에서 문항 생성 시 이미 부여한 `grammar_questions.topic`을 그대로 재사용**하기로 함 — 콘텐츠 저작 단계에서 이미 의미있는 문법 주제명이 붙어있어 중복 설계를 피함.
- **완료조건 검증**: `tests/test_grammar_flow.py`(정답/오답 기록, 오답유형 태그 저장, 하루 1회 제한, `/복습`은 제한 없이 가능) 통과. 이어서 실제 Supabase 콘텐츠로 실제 텔레그램 봇을 통해 5문항(정답3/오답2) 세션을 끝까지 진행해 `user_grammar_answers`에 정확한 오답유형 태그(예: "If 생략 가정법 도치", "동명사의 의미상 주어 및 수동 형태")와 함께 기록됨을 확인.

### M6 후속 개선 (사용자 실사용 피드백 반영)
실제 문법 학습을 써본 사용자가 두 가지를 지적함: (1) 개념 설명 없이 바로 문제만 나옴, (2) 문법 문장에 아직 배우지 않은 단어가 섞여 나옴. 둘 다 콘텐츠 양과 무관한 설계 누락이었음(기획서에도 명시되어 있지 않던 부분).
- **개념 설명 선행**: `grammar_questions.concept_intro` 컬럼 추가(`migrations/0006_grammar_concept_intro.sql`). 콘텐츠 생성 프롬프트(M4)를 확장해 문제마다 2~3문장 개념 설명을 함께 생성, 문제를 보여주기 전에 먼저 전송(확인질문 없이 자동 진행, 섹션7).
- **문법 문장의 단어를 단어학습과 연결**: 콘텐츠 생성 시 각 문법 문장에서 핵심 단어(key_vocabulary) 2~4개를 함께 추출해 `words` 테이블(같은 레벨)에 합류시킴. 별도 사용자별 AI 호출이나 새 연결 테이블 없이, `/단어학습`이 이미 "레벨별로 아직 안 배운 단어"를 자동으로 찾아오는 로직(`get_new_words`)을 그대로 활용 — 문법에서 나온 단어가 자연스럽게 다음 단어학습에 섞여 들어옴.
- 기존 39개 문법 문제(콘텐츠 재생성 없이)에도 `scripts/enrich_existing_grammar.py`로 위 두 필드를 소급 추가 — concept_intro 39/39 갱신, key_vocabulary 85개 단어 신규 삽입(레벨당 25~35개).
- **완료조건 검증**: 기존 테스트 확장(`tests/test_content_generator.py`에 concept_intro 필수화·key_vocabulary 필터링 케이스 추가, `tests/test_grammar_flow.py`에 개념 설명 선행 전송 케이스 추가) + 실제 텔레그램에서 개념 설명 → 문제 순서로 오는 것과 단어뱅크가 43→85개 이상으로 늘어난 것을 확인.

### 리플라이 키보드가 접혀서 메뉴 버튼이 안 보이는 문제 (사용자 실사용 피드백)
텔레그램 리플라이 키보드는 한 번 접으면(사용자가 키보드 아이콘을 눌러 숨기면) 그 뒤에 오는 메시지에 `reply_markup`이 없으면 계속 안 보인다. "이미 완료했습니다" 류의 안내 메시지들에 키보드를 안 붙이고 있어서, 실사용 중 사용자가 메뉴 버튼을 못 찾는 상황이 발생함.
- 세션이 비어있거나(`아직 학습할 문법 문제가 없습니다` 등) 콜백이 만료된 경우(`이미 처리된 문항이거나...`) 등 "막다른" 안내 메시지 전부에 `reply_markup=menu.build_main_menu_keyboard(...)`를 추가해 메뉴가 항상 다시 뜨도록 함.

### 영문 명령어 별칭 + 텔레그램 '/' 자동완성 메뉴 등록 (사용자 요청)
사용자가 "/를 치면 모든 명령어가 보이게 해달라"고 요청. 텔레그램의 `setMyCommands`는 명령어 이름이 영문 소문자/숫자/밑줄만 가능해 기존 한글 명령어(`/단어학습` 등)는 등록할 수 없음 — 영문 별칭(`/vocabstudy` 등)을 추가해 등록하고, 라우터가 한글/영문 둘 다 인식하도록 함.
- `app/menu.py`에 `GENERAL_COMMANDS`(전체 공개)/`ADMIN_ONLY_COMMANDS`(관리자 채팅에만, `scope`로 구분) 목록 정의.
- `app/telegram_client.set_my_commands`, `app/commands.register_bot_commands` 신규 추가. 서버 기동 시(`app/main.py` lifespan)와 로컬 폴링 스크립트 기동 시 자동 등록.
- **완료조건 검증**: `tests/test_commands.py`(명령어 이름이 텔레그램 규칙에 맞는지 정규식 검증) + `getMyCommands` API로 실제 등록 확인(일반 7개, 관리자 채팅에 +2개).

### M7 — 오늘의 학습 (Planner)
- `learning_sessions` 테이블(`migrations/0007_learning_sessions.sql`) — 사용자별/날짜별 세션 1건(unique), 완료 단계 목록(`stages_completed` jsonb), 시작/완료 시각. `ai_call_count` 컬럼은 M17(AI 사용량 조회)에서 실제로 채워 넣을 예정으로 미리 추가만 해둠.
- `/오늘학습`(`/todaylearning`, 🗓 오늘의 학습): 확인질문 없이 **단어 학습 → 문법 학습**을 자동으로 이어서 진행(섹션7 학습자동화 원칙). 각 단계가 끝나면(`app/today_session.py`의 프로세스 메모리 상태로 추적) 자동으로 다음 단계를 시작하고, 더 배울 단어가 없거나 문법을 오늘 이미 했다면 그 단계를 건너뛰고 다음으로 넘어간다.
- **"Planner AI 보정"(기획서 섹션8-2/16) 후속 구현**: 처음엔 데이터 부족을 이유로 생략했으나, 사용자가 "데이터 채우고 넘어가"라고 요청 — 이미 쌓인 문법 오답 기록(`user_grammar_answers.error_tag`)을 근거로 실제 구현함.
  - `app/planner.py`: 문항 구성(어떤 단어/문법 문제를 낼지)은 그대로 규칙기반이 결정하고, AI는 **오늘의 학습 시작 시 보여줄 1~2문장 포커스 메시지**만 만든다("AI는 하루 보정만, 규칙기반 우선" 원칙 그대로 유지). 취약 주제가 없으면(오답 기록 없음) AI를 아예 호출하지 않아 불필요한 비용을 피함.
  - `app/repo/grammar.py`에 `get_weak_topics`(오답유형 태그 빈도 상위 N개)/`get_overall_accuracy` 추가 — 별도 취약점 분석 테이블 없이 기존 `user_grammar_answers`만으로 계산.
  - `learning_sessions.ai_call_count`를 실제로 증가시키기 시작(M17 AI 사용량 조회에서 이 값을 그대로 집계 가능).
  - **완료조건 검증**: `tests/test_planner.py`(취약주제 없으면 None, 있으면 AI 호출 후 메시지 반환, AI 실패 시 조용히 건너뜀) + `tests/test_today_session.py`에 AI 메시지 노출/미노출 케이스 추가. 이어서 실제 Gemini + 실제 누적된 오답 데이터(동명사 수동태 2회, 가정법 도치 관련 2회, 정답률 50%)로 `/오늘학습`을 다시 실행해 "최근 동명사의 수동·의미상 주어와 가정법 도치 구문에서 아쉬운 오답이 많았어요..." 같은 실제 데이터 기반 개인화 메시지가 나오는 것과 `learning_sessions.ai_call_count=1`로 기록되는 것을 확인.
- 회화(M9)가 없어 세 단계(단어→문법→해석)까지지만, `today_session.STAGES` 목록만 확장하면 그대로 합류되는 구조로 설계 — 실제로 M8에서 `STAGES`에 `"reading"`만 추가해서 그대로 동작함.
- **완료조건 검증**: `tests/test_today_session.py`(단어→문법 자동 체이닝, 단어 큐가 비어있을 때 건너뛰기, 문법을 이미 했을 때 건너뛰기) 통과.
- **실사용 중 실제 버그 발견 및 수정**: `learning_sessions.stages_completed`(jsonb 컬럼) 갱신 시 `psycopg.types.json.Json`(→ Postgres `json` 타입으로 바인딩)을 썼는데, `jsonb || json` 연산자가 Postgres에 없어서 `UndefinedFunction` 에러로 단어 학습 완료 시점에 플로우가 멈추는 문제가 실제 텔레그램 테스트 중 발생함. `Jsonb`(→ `jsonb` 타입으로 바인딩) 래퍼로 교체해 해결. 유닛테스트는 DB를 모킹하기 때문에 이런 타입 불일치는 잡아내지 못했고, 실제 Supabase 연동 테스트에서만 드러남 — 이후 유사한 jsonb 컬럼 작업 시 `Json` 대신 `Jsonb`를 쓰도록 주의.
- **최종 End-to-End 검증**: 실제 사용자가 단어 개수가 많아(신규 단어 자동증가로 7개) 수동 클릭이 번거롭다고 요청 → `scripts/simulate_today_session.py`(1회성, 실제 DB/실제 세션 로직을 그대로 타되 텔레그램 발송만 캡처)로 대신 자동 진행: 단어 7개 전부 성공 → 문법은 이미 오늘 완료되어 자동 스킵 → "오늘의 학습을 모두 마쳤습니다!"까지 확인질문 없이 도달. `learning_sessions`에 `stages_completed=["vocab"]`(문법은 스킵되어 목록에 안 남음), `completed_at` 정확히 기록됨을 DB에서 직접 확인.

### M8 — 해석 (Reading)
- `reading_passages`/`user_reading_attempts` 테이블(`migrations/0008_reading.sql`) — 지문+모범번역 콘텐츠뱅크, 사용자별 시도 기록(시도횟수/적절여부/AI피드백/복습여부).
- 콘텐츠 생성(M4 확장, `app/content/generator.generate_reading_passages`): 레벨별 영어 지문(3~5문장) + 한국어 모범번역을 함께 생성. `scripts/generate_content_bank.py`에 `--reading-per-level` 옵션 추가, 레벨당 5개씩 총 15개 생성.
- `/해석`(`/reading`, 📖 해석(Reading)): 지문을 보여주고 사용자가 한국어로 직접 해석해서 답장 → **핵심 차별점(섹션2) "정답을 먼저 안 줌" 구현**: AI가 채점하되 처음엔 정답 대신 힌트만 주고(`app/reading/evaluator.py`), 최대 2회까지 재시도 기회를 준 뒤에야 모범번역을 공개. 답이 처음부터 적절하면 바로 공개.
  - 평가 로직은 프롬프트에 "정답을 절대 직접 알려주지 마라"를 명시하되, 서버 코드가 `is_adequate`/시도횟수를 기준으로 **모범번역 노출 여부를 직접 통제**해서 AI가 실수로 답을 흘려도 사용자에게 노출되지 않도록 이중 안전장치를 둠(모범번역 자체는 AI 응답에 포함시키지 않고 DB에 미리 저장된 값을 서버가 별도로 붙임).
  - AI 평가 실패(API 오류 등) 시 무한 대기시키지 않고 "채점 중 문제가 발생했습니다" 안내와 함께 모범번역을 공개하고 종료(fail-open) — 섹션19-8 "기존 기능을 깨뜨리지 않도록" 원칙에 따라 장애가 사용자를 막다른 골목에 가두지 않게 함.
  - 하루 1회 제한은 적용하지 않기로 함(섹션18 [결정필요] 유사 판단): 문법 MCQ와 달리 해석 연습은 다른 지문으로 반복해도 자연스러워, 매번 새 지문을 무제한 제공. `/복습`은 여전히 문법 전용으로 유지(README/M6 문서 참고).
- **완료조건 검증**: `tests/test_reading_flow.py`(정답 즉시 처리, 1차 힌트 후 2차 정답공개, 정답이 힌트에 노출 안 됨, 지문 없음 처리, AI 실패 시 fail-open) + `tests/test_today_session.py`에 문법→해석 자동 체이닝 케이스 추가. 이어서 `scripts/simulate_reading.py`로 실제 Gemini + 실제 DB를 통해 (1) 부실한 해석 2회 제출 시 1차는 힌트만(정답 미노출), 2차는 정답 공개, (2) 모범번역을 그대로 제출하면 한 번에 정답 처리되는 것을 확인. `user_reading_attempts`에 시도별 기록(적절여부/시도번호) 정확히 저장됨을 DB에서 직접 확인.

### M9 — 회화 (Conversation)
- `conversation_sessions`/`conversation_messages` 테이블(`migrations/0009_conversation.sql`) — 세션별 레벨/턴수/시작·완료시각, 메시지별 role(user/model)+내용.
- `/회화`(`/conversation`, 💬 회화(Conversation)): AI와 텍스트로 5턴 대화(섹션3-1). `app/conversation/chat.py`가 매 턴 전체 대화 기록을 프롬프트에 실어 `generate_text`를 호출하는 방식으로 구현(genai의 `start_chat` 객체를 프로세스 메모리에 계속 들고 있지 않고, 다른 세션들과 동일하게 순수 데이터(문자열 튜플 리스트)로 기록해 재구성 — 재시작/스케일아웃에 더 안전).
- 학습자 레벨을 프롬프트에 명시해 난이도를 맞춤(정확한 "아는 단어 목록 제약"까지는 구현하지 않고 레벨 설명 수준으로 근사 — 엄밀한 어휘 제약은 비용 대비 효과가 낮다고 판단).
- **하루 1회 제한을 그대로 적용**(해석과 달리 예외를 두지 않음): 회화는 턴마다 AI를 호출해 세션당 최대 6회(오프닝 1 + 답장 5) 호출되는, 지금까지 만든 기능 중 가장 AI 호출이 많은 기능이라 섹션16 "예산모드" 원칙상 스펙 그대로 하루 1회로 비용을 통제. `/복습`은 여전히 문법 전용 유지.
- AI 응답 실패 시(레이트리밋 등) 대화가 끊기지 않도록 자연스러운 대체 응답으로 넘어가고, 턴 수는 정상적으로 채워 세션을 완료 처리(fail-open, 해석과 동일한 원칙).
- 오늘의 학습(M7) 체인의 마지막 단계로 합류 — 이제 단어→문법→해석→회화 전체가 확인질문 없이 자동으로 이어진다.
- **완료조건 검증**: `tests/test_conversation_flow.py`(하루1회 차단, 5턴 완료 후 종료, AI 실패해도 대화 유지) + `tests/test_today_session.py`에 회화 스킵 처리 반영. 이어서 `scripts/simulate_conversation.py`로 실제 Gemini + 실제 DB를 통해 5턴 전체 대화(레벨에 맞는 학술적 어휘로 자연스럽게 진행됨)와 하루 1회 제한이 실제로 작동하는 것을 확인.

#### ⚠️→✅ Gemini 무료 티어 일일 한도 문제 — 원인 파악 및 해결
회화 시뮬레이션 6번째(마지막) AI 호출에서 실제로 `429 ResourceExhausted`를 받음 — 에러 메시지에 따르면 **`gemini-3.6-flash` 모델의 무료 티어 일일 한도는 하루 20회**(`GenerateRequestsPerDayPerProjectPerModel-FreeTier`, `quota_value: 20`)였음. 기획서 섹션16은 "무료 티어 한도는 하루 약 1,500회"를 전제로 하는데 그 1/75 수준이라 사용자와 함께 원인을 조사함.

**원인**: `gemini-3.6-flash`는 2026년 7월에 새로 나온 최신 모델이라 아직 표준 무료 티어(하루 1,500회)를 받지 못하고, 훨씬 제한적인 도입 초기 할당량(20회)만 제공되고 있었음. 반면 같은 세대의 `gemini-3.1-flash-lite`는 표준 무료 티어 목록에 포함되어 있어 하루 1,500회를 그대로 받음 — 실제로 `gemini-3.6-flash`의 20회 한도를 소진한 상태에서도 `gemini-3.1-flash-lite`는 즉시 정상 호출됨을 확인해, **무료 티어 할당량이 모델별로 독립적으로 관리된다**는 것도 함께 확인함.
- **조치**: `app/ai/gemini_client.py`의 기본 모델을 `gemini-3.6-flash` → **`gemini-3.1-flash-lite`**로 교체(JSON 모드 포함 정상 동작 확인). 이 프로젝트의 모든 AI 호출(`generate_text`/`generate_json`)이 이 한 곳을 거치므로 다른 코드 변경 없이 전체가 새 한도로 전환됨.
- **검증**: 실제 API로 `gemini-3-flash`(존재하지 않음)/`gemini-3.1-flash-lite`(텍스트+JSON 모드 모두 성공)/`gemini-2.5-flash-lite`(구모델, 신규사용자 불가)를 직접 호출해 비교, 앱의 실제 `generate_text` 함수로 최종 확인.
- **후속 조치(M17 AI 사용량 조회에서 다룰 예정)**: `learning_sessions.ai_call_count`로 세션별 AI 호출 수 추적 기반은 이미 마련되어 있음. 향후 더 최신 모델(3.6/3.7/3.8 등)이 표준 무료 티어에 포함되면 다시 교체를 검토할 수 있고, 실사용자가 늘면 유료 티어 전환도 고려.

### M10 — 복습/진도 (`/진도`, 관리자 사용자 목록)
- `/복습`은 M6에서 이미 구현됨(문법 전용). 이번 마일스톤은 `/진도`(📊 진도 확인)와 관리자용 사용자 현황을 추가.
- `/진도`(`/progress`): 레벨/단어(완전히 익힘·학습중 개수)/문법(정답률·취약주제)/해석(시도·적절판정 횟수)/회화(완료 세션 수)를 한 화면에 요약(섹션12 표 구조 반영). 새 테이블 없이 기존 테이블(`user_words`, `user_grammar_answers`, `user_reading_attempts`, `conversation_sessions`)만 집계.
- 관리자 `/사용자목록`(`/users`): 사용자 실사용 피드백 반영 — 승인된 사용자 전체를 아이디/나이/학습모드·레벨/**오늘 활동 횟수**/최근접속으로 한 번에 조회(섹션4-2 "사용자 관리"). 오늘 활동 횟수는 문법답변+해석시도+회화메시지+단어복습 합산으로 계산(정확한 "메시지 전송 횟수"는 아니고 학습 액션 수 기준 근사치 — 결정필요 항목으로 문서화).
- **관리자 하루 제한 우회**: 사용자 요청("나는 모든 제한 풀어줘, 하루 몇 번 제한은 다른 사람만") 반영 — `role == 'admin'`인 사용자는 문법학습/회화의 하루 1회 제한을 건너뛴다. ⚠️ 회화는 세션당 최대 6회 AI 호출인데, Gemini 무료 티어가 프로젝트 전체 공유로 하루 20회뿐이라(위 M9 리스크 항목 참고) 관리자가 이 우회를 자주 쓰면 다른 실사용자 몫까지 빨리 소진될 수 있음 — 관리자 본인에게 사용 시 유의 필요하다고 안내함.
- 관리자 명령어 가시성은 이미 M9 이전부터 안전하게 분리되어 있었음(재확인): `⚙️ 관리자` 버튼은 `is_admin`일 때만 메뉴에 추가되고, `/pending`/`/approve`/`/users` 텍스트 명령은 `telegram_id == ADMIN_TELEGRAM_ID` 체크를 통과해야만 실행되며, 텔레그램 `/` 자동완성에도 관리자 채팅에만 별도 scope로 노출됨(`getMyCommands`로 실측 확인됨, M6 이전 절 참고) — 다른 사용자에게는 노출도 실행도 되지 않음.
- **완료조건 검증**: `tests/test_progress.py`(데이터 있을 때/없을 때 진도 리포트, 관리자 사용자 목록 표시) + `tests/test_grammar_flow.py`·`tests/test_conversation_flow.py`에 관리자 하루제한 우회 케이스 추가. pytest 52건 통과.

### M11 — 개인 텍스트 붙여넣기 학습
- `custom_texts` 테이블(`migrations/0010_custom_texts.sql`) — 섹션3-2 "원문 텍스트는 전체 저장하지 않고 핵심만 추출 저장" 원칙에 따라 **원문 전체를 저장하지 않고** 앞부분 200자(`source_excerpt`)와 전체 글자수(`source_char_count`)만 남긴다. 사용자의 해석 시도(`user_translation`)와 AI 피드백은 사용자 본인이 작성/받은 내용이라 원문 저장 제한과 무관하게 그대로 저장.
- `/텍스트학습`(`/customtext`, 📄 텍스트 붙여넣기 학습): 텍스트 전송 → 핵심 단어 추출(최대 8개, AI 1회 호출) → **기존 단어학습 카드 플로우를 그대로 재사용**해서 아는단어/모르는단어→객관식→주관식 진행 → 전체 텍스트 해석 요청 → AI 피드백(정답을 대신 번역해주지 않고 2~3문장 코멘트만, AI 1회 호출) → 완료.
- 추출된 단어는 M6의 문법 핵심단어 추출과 동일한 방식으로 **공용 `words` 테이블(사용자 레벨 기준)에 합류**시켜, 별도의 개인 단어장 구조 없이도 이후 `/단어학습`에서 자연스럽게 다시 만나게 된다("단어는 개인 단어장(SRS)에 계속 저장"이라는 스펙 취지를 기존 인프라 재사용으로 충족).
- 카드 학습 단계는 `_finish_vocab_word`에 분기를 추가해 구현 — 오늘의 학습(M7) 체이닝과 같은 패턴으로, 단어 카드가 끝나면 자동으로 해석 요청 단계로 넘어간다(확인질문 없이, 섹션7). 다만 이 기능 자체는 오늘의 학습(M7)의 정규 단계에는 포함하지 않음(사용자가 직접 텍스트를 골라 붙여넣는 사용자 주도 기능이라 자동 스케줄에 넣지 않기로 함 — 결정필요 항목).
- 하루 횟수 제한 없음(단어학습과 동일하게 무제한) — 다만 세션당 AI 호출이 2회(단어추출+피드백)라 무료 티어 한도를 고려해 남용 시 소진 위험은 해석/회화와 동일.
- **완료조건 검증**: `tests/test_custom_text_flow.py`(단어 추출 후 카드→해석 전체 플로우, 추출 단어 없을 때 카드 단계 스킵, 너무 짧은 텍스트 거부) + `tests/test_menu.py` 갱신(모든 메뉴 버튼이 이제 구현되어 NOT_YET_IMPLEMENTED가 비었음을 확인하는 테스트로 교체). 이어서 `scripts/simulate_custom_text.py`로 실제 Gemini + DB를 통해 실제 기사 문단에서 단어 6개 추출 → 카드 전부 처리 → 해석 제출까지 확인. 마지막 피드백 생성 호출에서 오늘 누적된 Gemini 무료 한도(20회/일)를 초과했지만 fail-open으로 자연스럽게 완료 처리됨을 확인 — `custom_texts`에 원문 200자 발췌본(전체 267자 중)과 실제 글자수, 추출 단어 수(6), 사용자 해석, 대체 피드백이 정확히 저장됨을 DB에서 직접 확인.

### M12 — 학교 수행평가(단일 글) 텍스트 학습
- `school_assignments` 테이블(`migrations/0011_school_assignment.sql`) — M11(`custom_texts`)과 동일하게 원문 전체는 저장하지 않고 앞부분 200자(`source_excerpt`)와 전체 글자수만 남긴다. 여기에 문법해설 텍스트와 예상 시험문제 결과(문항수/정답수)를 추가로 저장.
- `/수행평가`(`/schooltask`, 📝 학교 수행평가): M11(텍스트 붙여넣기 학습)과 시작 부분(텍스트 붙여넣기 → 핵심단어 추출 → 카드 학습)은 동일한 코드를 재사용하되, 그 뒤에 **전체 해석 대신** 다음 두 가지로 마무리한다(기획서 요건: "M11과 비슷하지만 문법해설+예상시험문제 생성까지").
  1. **핵심 문법 해설**: 지문에 실제 쓰인 문법 포인트 2~3개를 지문 속 문장을 인용해 한국어로 설명.
  2. **예상 시험문제**: 지문 내용/문법/어휘를 바탕으로 한 4지선다 객관식 3문항 — 기존 문법학습(M6)과 동일한 인라인 키보드 채점 흐름을 재사용.
  - 두 가지 모두 **한 번의 Gemini JSON 호출**(`app/school_assignment/analyzer.py`)로 함께 생성 — 별도 호출로 나누지 않아 세션당 AI 호출 수를 단어추출(1회)+분석(1회)=2회로 M11과 동일하게 유지(섹션16 AI 호출 최소화 원칙).
  - 예상문제 생성이 실패하거나 형식이 어긋나 0문항이 되어도(fail-open) 문법 해설까지는 그대로 보여주고 세션을 정상 종료한다.
- 세션 상태는 다른 학습 세션과 동일하게 프로세스 메모리(`app/school_assignment/service.py`)로 관리(소수 사용자 규모, 섹션3-2). 하루 횟수 제한 없음 — 시험 대비 특성상 여러 지문을 반복해서 준비할 수 있어야 한다고 판단.
- 이 기능은 오늘의 학습(M7) 자동 체인에는 포함하지 않음 — M11(텍스트 붙여넣기 학습)과 동일하게 사용자가 특정 수행평가 지문을 직접 골라 붙여넣는 능동적 기능이라 정규 자동 루틴과는 성격이 다르다고 판단(M11과 동일한 결정 근거 재사용).
- **완료조건 검증**: `tests/test_school_assignment_flow.py`(단어 추출 후 카드→문법해설→예상문제 전체 플로우, 추출 단어 없을 때 카드 단계 스킵, 너무 짧은 지문 거부, 예상문제 생성 실패 시 문법해설까지만 완료 처리) 통과, 전체 pytest 60건 통과. 이어서 `scripts/simulate_school_assignment.py`로 실제 Gemini + 실제 Supabase를 통해 산호초 백화현상 지문으로 단어카드(1개) → 문법해설(동격의 콤마/주격 관계대명사) → 예상문제 3문항(오답1/정답1/오답1) 순으로 끝까지 진행, `school_assignments`에 `source_char_count=315`, `extracted_word_count=1`, `exam_question_count=3`, `exam_correct_count=1`이 정확히 저장됨을 DB에서 직접 확인.

## 진행 중 / 다음 단계

- **M13 학교 시험 관리 확장**(시험/단원/자료/선생님강조/D-Day/시험직전복습)부터는 아직 미구현.
- 실제 텔레그램/Supabase/Gemini 계정 연동 테스트는 완료됨 (아래 "실제 계정 연동 후 End-to-End 검증" 참고).

## 이번 구간에서 정한 기본값 (섹션 18 `[결정 필요]` 관련, 섹션19-7에 따라 개발 중단 없이 진행)

| 항목 | 정한 기본값 | 근거 |
|---|---|---|
| DB 드라이버 | asyncpg 대신 psycopg3(+psycopg-binary) | 현재 개발 환경(Python 3.14/Windows)에서 asyncpg는 사전 빌드된 wheel이 없어 컴파일이 필요한데 빌드 도구가 없음. psycopg-binary는 사전 컴파일된 wheel 제공. 기능적으로 동일(Postgres 비동기 접근) |
| 승인 대기/나이입력 상태 | 별도 "대화 상태" 테이블 없이 `users.approved`/`users.learning_mode` 값 자체로 판단 | 소수 사용자 규모(섹션3-2)에서 상태 테이블은 과설계. 컬럼이 null인지 여부로 충분히 분기 가능 |
| 관리자 등록 방식 | 관리자 최초 `/start` 시 자동 승인+role 부여 | 기획서에 명시적 절차가 없어, "관리자는 이미 신뢰된 존재"라는 섹션4-2 취지에 맞춰 최소 마찰로 처리 |
| 레벨진단 문항 수 | 20/10/지문1 대신 5/5(지문 제외)로 축소 | 채점 로직은 문항 수와 무관하게 동일. 실제 20/10 문항 저작은 콘텐츠 작업이며 M4(콘텐츠뱅크)에서 규모를 키울 예정 |
| 레벨진단 세션 저장소 | DB 테이블 대신 프로세스 메모리 dict | 소수 사용자·비고가용성 요건(섹션3-2). 서버 재시작 시 진행 중이던 진단만 초기화되고 `/레벨진단`으로 재시작 가능해 리스크가 낮음 |
| 레벨 이름 | beginner/intermediate/advanced (정답률 40%/80% 기준) | 기획서에 레벨 이름·경계값이 명시되어 있지 않아 임의로 정함. 추후 실사용 데이터로 조정 필요 |
| words/grammar_questions 스키마 | 최소 필드로 직접 설계(섹션11-2 표 참고) | V1 문서가 없어 정확한 필드를 알 수 없음. 학습 카드 표시(뜻/발음/예문)와 채점(정답 인덱스)에 필요한 최소 필드만 우선 정의, 부족하면 이후 마일스톤에서 컬럼 추가 |
| 콘텐츠뱅크 초기 규모 | 레벨당 단어 15개, 문법 10개 (총 45+30) | 기획서에 정확한 수량 명시가 없음. M5(단어학습) 로직 검증에는 충분하고, Gemini 무료티어 비용 부담도 적은 규모로 시작. 실사용 중 부족하면 스크립트를 다시 실행해 늘릴 수 있음(중복 단어는 자동 스킵) |
| 신규 단어 수 자동조절 기준 | 정답률 90%↑ +1(최대15), 50%↓ -1(최소3) | 기획서 섹션18 [결정필요] 항목. 실사용 데이터가 쌓이면 조정 |
| 단어시험 샘플 개수 | 5개, 객관식만 | 기획서 섹션18 [결정필요]. 반복 응시 가능하므로 매번 크게 줄 필요 없다고 판단 |
| 단어 주관식 채점 방식 | 문자열 유사도(difflib) 기반, AI 없음 | 처음엔 단순 부분일치였으나 실사용 중 정답을 오답 처리하는 버그 발견 → 유사도 비교로 개선 (AI 호출 최소화 원칙은 유지) |
| 문법 세트 크기 | 5문항 | 기획서에 명시 없음. 단어시험(5개)과 동일하게 맞춰 일관성 확보 |
| 문법 오답 유형 태그 | `grammar_questions.topic` 재사용 | 기획서 섹션18 [결정필요]. 별도 태그 체계를 새로 정의하는 대신 M4에서 이미 부여한 문법 주제명을 그대로 활용 |
| `/복습`의 범위 | 현재는 문법만 | 해석(M8)/회화(M9)가 아직 없어 자연스럽게 문법 전용. 추후 구현되면 확장 예정 |
| 오늘의 학습 Planner의 "AI 보정" 범위 | 문항 구성은 규칙기반, AI는 시작 시 포커스 메시지 1개만 생성 | 기획서 섹션16 "AI는 하루 보정만, 규칙기반 우선" 원칙에 맞춰 최소 범위로 구현. 취약주제 없으면 AI 호출 자체를 생략 |
| jsonb 컬럼 갱신 시 psycopg 래퍼 | `Json` 대신 `Jsonb` 사용 | 실사용 중 발견: `Json`은 Postgres `json` 타입으로 바인딩되어 `jsonb || json` 연산에서 타입 오류 발생. `Jsonb` 래퍼로 교체해 해결 |
| 해석(Reading) 재시도 횟수 | 최대 2회(1차 힌트, 2차 정답공개) | 기획서에 구체적 횟수 명시 없음. 너무 많으면 지치고 1회면 "힌트→재시도"가 불가능해 2회로 절충 |
| 해석(Reading) 하루 1회 제한 | 적용하지 않음(언제나 새 지문 가능) | 문법 MCQ와 달리 매번 다른 지문으로 반복 연습하는 게 자연스러운 학습 방식이라 판단. `/복습`은 기존대로 문법 전용 유지 |
| 회화(Conversation) 하루 1회 제한 | 기획서 그대로 적용(신규 세트 하루 1회) | 세션당 최대 6회 AI 호출로 지금까지 만든 기능 중 비용이 가장 큼 — 실측 결과 Gemini 무료 티어가 하루 20회로 매우 타이트해서(아래 리스크 항목 참고) 예외를 두지 않고 스펙대로 제한 |
| 관리자용 "오늘 활동 횟수" 계산 방식 | 문법답변+해석시도+회화메시지+단어복습 건수 합산 | 기획서에 정확한 정의가 없음. "메시지 전송 횟수" 자체를 로깅하는 테이블이 없어, 기존 학습 기록 테이블들의 오늘자 행 수를 합산하는 근사치로 구현 |
| 관리자 하루 제한 우회 | `role='admin'`이면 문법학습/회화 하루1회 제한 면제 | 사용자(관리자 본인) 요청. 일반 사용자에게는 계속 제한 적용 |
| 텍스트 붙여넣기 학습(M11) 추출 단어 개수 | 최대 8개 | 기획서에 개수 명시 없음. 프롬프트/토큰 크기와 카드 학습 소요시간을 고려해 제한 |
| 텍스트 붙여넣기 학습을 오늘의 학습(M7) 자동 체인에 포함할지 | 포함하지 않음 | 사용자가 직접 글을 골라 붙여넣는 능동적 기능이라, 매일 자동으로 도는 정규 루틴에 넣기보다 사용자가 원할 때 별도로 실행하는 기능으로 유지 |
| 학교 수행평가(M12) 예상 시험문제 개수 | 3문항 | 기획서에 개수 명시 없음. 문법학습 세트(5문항)보다는 짧게, 단어시험(5문항)과 유사한 감각으로 지문 하나당 부담 없이 절충 |
| 학교 수행평가(M12) 문법해설+예상문제 생성 방식 | 한 번의 Gemini JSON 호출로 두 가지를 함께 생성 | AI 호출 최소화 원칙(섹션16)에 따라 별도 호출로 나누지 않음 — 세션당 AI 호출 수를 M11(단어추출+피드백 2회)과 동일하게 유지 |
| 학교 수행평가(M12) 하루 횟수 제한 | 적용하지 않음 | 시험 대비 특성상 여러 지문을 준비 기간 동안 반복 학습할 수 있어야 한다고 판단. M11(텍스트 붙여넣기 학습)과 동일한 근거 |
| 학교 수행평가(M12)를 오늘의 학습(M7) 자동 체인에 포함할지 | 포함하지 않음 | M11과 동일 — 사용자가 특정 수행평가 지문을 직접 골라 붙여넣는 능동적 기능 |

## 실제 계정 연동 후 End-to-End 검증 (2026-09-07)

사용자가 실제 Bot Token/관리자 ID/Gemini Key/Supabase 프로젝트를 제공해 아래를 실제로 검증함:

- **Telegram Bot Token**: `getMe`로 유효성 확인 (봇: `@KR_My_Englishbot`).
- **Gemini API Key**: 유효하나 기획서의 `gemini-2.5-flash`는 신규 사용자에게 더 이상 제공되지 않아 `gemini-3.6-flash`로 교체 ([app/ai/gemini_client.py](app/ai/gemini_client.py)).
- **Supabase**: 무료 티어의 **Direct Connection(`db.<ref>.supabase.co`)은 IPv6 전용**이라 일반 네트워크에서 DNS 조회부터 실패함. **Session Pooler**(`aws-0-<region>.pooler.supabase.com:5432`, IPv4)로 전환해 해결. 실제 마이그레이션(0001, 0002) 적용 완료.
- **실제 텔레그램 대화로 전체 플로우 검증**: 관리자 `/start` → 자동 승인 → 나이(17) 입력 → GENERAL 모드 배정 → 레벨진단 자동 시작 → 인라인 키보드로 10문항 응답 → 결과 저장(단어5/5, 문법4/5 → `advanced`)까지 실제 DB(`users`, `placement_test_results`)에 정확히 반영됨을 확인.

### 로컬 개발 환경(Windows) 전용 이슈 2건과 해결

1. **psycopg 비동기 모드 vs Windows ProactorEventLoop**: Windows의 기본 이벤트루프는 psycopg 비동기 모드를 지원하지 않음. `app/main.py` 안에서 정책을 바꾸는 시도는 uvicorn이 이미 루프를 만든 뒤라 효과가 없어서, 이벤트루프 생성 전에 정책을 바꾸는 별도 실행 스크립트 [run_local.py](run_local.py)를 추가함. **배포 환경(Linux/Railway)에는 이 이슈 자체가 없음.**
2. **로컬에서 실제 텔레그램 대화 테스트**: ngrok 미설치 상태라 webhook 대신 [scripts/poll_dev.py](scripts/poll_dev.py)(getUpdates long polling)로 대체 검증. 실제 배포 시에는 여전히 webhook(`app/main.py`)을 사용.

## 알려진 제약

- Docker Desktop이 로컬에 설치되어 있으나 실행 중이 아니어서 로컬 Postgres 컨테이너 방식은 시도하지 않음(대신 실제 Supabase로 바로 검증함).
- 아직 Railway 배포 및 webhook 등록(ngrok 또는 실제 배포 URL)은 진행하지 않음 — 로컬 polling 방식으로만 실사용 검증됨.
