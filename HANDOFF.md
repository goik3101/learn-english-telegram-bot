# 세션 인수인계 문서

새 대화(새 탭)에서 이어서 작업할 때 이 문서를 먼저 읽으면 바로 맥락을 파악할 수 있다.
**상세 내역은 [IMPLEMENTATION_STATUS.md](IMPLEMENTATION_STATUS.md)(마일스톤별 완료 내역/근거/버그/결정사항)와 [README.md](README.md)(설치/실행/명령어 목록)에 이미 다 있음 — 이 문서는 그 둘의 요약 + 지금 당장 알아야 할 것만 모은 진입점이다.**

## 한 줄 요약

기획서(가족용 텔레그램 영어학습 봇, 관리자 승인제) 기준 **기획서 로드맵(M0~M17) 전부 구현 완료** + 로드맵 이후 사용자 추가 요청 **M18(개인 맞춤 난이도 시스템)/M19(회화 사전 단어학습)/M20(실사용 버그리포트 6건)** 까지 구현+검증 완료. pytest 160건 통과. **Render(무료 티어)에 실제 배포됨** — `@KR_My_Englishbot`으로 실사용 중.

## ⚠️ 배포 서버에 아직 반영 안 됐을 수 있는 것 (다음 탭에서 확인할 것)

M20(2026-09-10) 작업분이 로컬에는 다 있지만, **Render에 재배포되고 Supabase에 마이그레이션이 실제로 적용됐는지는 이 세션에서 확인 안 함** — 다음에 이어서 할 때 먼저 확인:
1. `git push`로 최신 커밋이 GitHub(Render 자동배포 트리거)에 올라갔는지.
2. `psql "$DATABASE_URL" -f migrations/0022_grammar_curriculum_and_vocab_memory.sql` 적용됐는지 — 이걸 안 하면 `users.grammar_topic_index` 리셋이 안 되고, `words.mnemonic`/`example_sentences`/`user_words.review_count` 컬럼이 없어서 단어학습이 통째로 에러날 수 있음(마이그레이션 필수, 선택 아님).
3. (선택) `python scripts/backfill_word_mnemonics.py`로 기존 단어에 연상법/복수예문 소급 부여 — 안 해도 새 단어부터는 정상 동작, 옛 단어는 그냥 연상법 없이 표시됨.

## 지금 당장 알아야 할 것

1. **Gemini 모델은 `gemini-3.1-flash-lite`를 쓴다** (`app/ai/gemini_client.py`). 처음엔 `gemini-3.6-flash`를 썼다가 무료 티어 일일 한도가 프로젝트당 **20회**밖에 안 된다는 걸 실측으로 발견함(최신 모델이라 아직 표준 무료 티어를 못 받음). `gemini-3.1-flash-lite`는 표준 무료 티어(하루 1,500회)에 포함되어 있고, 실제 문법문제 생성 프롬프트로 품질 비교해봐도 손색없어서 이걸로 교체함. **무료 티어 한도는 모델별로 독립적으로 관리된다** — 나중에 더 최신 모델로 바꾸고 싶으면 먼저 직접 호출해서 한도부터 확인할 것.
2. **로컬 실행 시 Windows 전용 이슈 2개**: (1) `uvicorn` 대신 `python run_local.py` 사용(psycopg 비동기가 기본 이벤트루프를 지원 안 함), (2) DB는 Supabase Direct Connection(IPv6 전용, 로컬에서 접속 안 됨) 대신 **Session Pooler** 문자열을 써야 함(`.env`에 이미 반영됨). 배포 환경(Linux/Render)에는 이 이슈 자체가 없음.
3. **실제 계정 이미 다 세팅되어 있음**: Telegram Bot Token(`@KR_My_Englishbot`), 관리자 텔레그램 ID, Supabase 프로젝트, Gemini API Key, Google Cloud TTS API Key 전부 `.env`(로컬)과 Render "Environment" 탭(배포)에 있음. 값 자체는 여기 다시 안 적음. **Google Cloud TTS 키는 Cloud Console "사용자 인증 정보 만들기" → 반드시 "API 키" 옵션이어야 함**(`AIzaSy...` 형식) — OAuth 클라이언트 ID나 Google AI Studio 스타일 키(`AQ.Ab8R...`)를 실수로 줄 수 있으니, 나중에 키를 다시 받을 일이 있으면 형식부터 확인할 것.
4. **배포는 Railway가 아니라 Render로 감** — Railway 무료 트라이얼이 끝나서 전환함. Render 무료 티어는 15분 미사용 시 슬립되어 첫 메시지 응답이 최대 50초 정도 느릴 수 있음(정상 동작, 결함 아님). webhook 방식(`app/main.py`)이고, `scripts/set_webhook.py`로 등록해둠.

## 로컬에서 바로 이어서 작업하는 법

```bash
python run_local.py              # 서버 실행 (Windows)
python scripts/poll_dev.py       # ngrok 없이 실제 텔레그램과 대화 테스트 (long polling)
pytest                           # 유닛테스트 160건
```

특정 기능을 수동 클릭 없이 끝까지 자동으로 검증하고 싶으면 `scripts/simulate_*.py` 스크립트들을 참고(오늘의학습/해석/회화/텍스트학습 각각 있음) — 실제 DB+Gemini를 타되 텔레그램 발송만 캡처해서 자동 진행.

## 구현 완료 (M0~M17 로드맵 전부 + M18/M19/M20) 한눈에

| 마일스톤 | 핵심 내용 |
|---|---|
| M0~M2 | 프로젝트 뼈대, 관리자 승인, 나이→모드(GENERAL/CHILD_BRIDGE/CHILD_BEGINNER) 배정 |
| M3 | 레벨진단(V0), 인라인 키보드+콜백쿼리 구조 확립(이후 전부 재사용) |
| M4 | 단어/문법/해석 콘텐츠뱅크 AI 대량생성 스크립트 |
| M5 | 단어학습(카드→객관식→주관식) + SRS + 단어시험 |
| M6 | 문법학습(개념설명→객관식) + 오답기록, `/복습` |
| M7 | 오늘의학습(단어→문법→해석→회화 자동 체이닝) + Planner AI 포커스 메시지 |
| M8 | 해석(Reading) — 정답 안 주고 힌트→재시도→공개 |
| M9 | 회화(Conversation) — AI와 5턴 대화, 하루 1회 |
| M10 | `/진도`(개인 통계), 관리자 `/사용자목록`(아이디/나이/모드/오늘활동), 관리자 하루제한 우회 |
| M11 | `/텍스트학습` — 텍스트 붙여넣기 → 핵심단어 카드학습 → 전체해석 → AI피드백 |
| M12 | `/수행평가` — 학교 수행평가 지문 붙여넣기 → 핵심단어 카드학습 → 핵심문법 해설 → 예상 시험문제 3문항(객관식) |
| M13 | `/시험등록`·`/시험목록`·`/시험직전복습` — 과목/D-Day/단원/선생님강조 등록, `/수행평가` 자료를 시험에 자동/선택 연결, 오답만 다시 출제하는 시험직전복습 |
| M14 | CHILD_BRIDGE(11세) 모드 — 메뉴/기능은 GENERAL과 완전 동일, 콘텐츠뱅크(`learning_mode` 컬럼)와 AI 프롬프트 톤만 분리해 쉬운 난이도+이모지+안전한 주제로 조정 |
| M15 | CHILD_BEGINNER(10세 이하) 모드 Stage0~6(전 단계) — GENERAL/CHILD_BRIDGE와 **완전히 분리된 전용 메뉴**(`app/child_menu.py`, 🎈오늘공부하기/⭐내진도 2버튼뿐), 알파벳/파닉스는 고정 커리큘럼, 기초단어/기초문장/QA/짧은지문은 AI 사전생성 콘텐츠뱅크, Stage6(듣기말하기)는 Stage2 콘텐츠를 TTS로 재사용 + 음성메시지 수신으로 말하기 연습(STT 없이 녹음 자체를 완료 처리) |
| M16 | TTS 캐싱 — Google Cloud TTS(API 키 방식) + `tts_cache` 테이블로 같은 텍스트 재합성 방지. 단어 카드(M5/M11/M12 공통)에 "🔊 발음 듣기" 버튼 추가, 카드를 지우지 않는 예외 콜백으로 처리. 실제 키로 합성+캐싱+텔레그램 음성메시지 전송까지 E2E 검증 완료 |
| M17 | AI 사용량 모니터링 — `ai_usage_log` 테이블 + `gemini_client`/`tts_client` 클라이언트 레벨 계측(호출부마다 추가할 필요 없이 자동 집계)으로 프로젝트 전체 Gemini/TTS 호출수 추적. 관리자 명령 `/AI사용량`으로 오늘/이번달 조회 |
| M18 | 개인 맞춤 난이도 시스템(로드맵 이후 사용자 추가 요청) — 단어(frequency_rank 밴드), 문법(고정 커리큘럼 주제별 게이팅+취약주제 가중복습), 독해(지문 난이도 밴드), 회화(독립적 CEFR conversation_level+세션후 피드백+응답 난이도 사후검사) 4개 영역 전부 최소학습량 10개+정답률 80%/50% 규칙 공유(`app/difficulty.py`) |
| M19 | 회화 사전 단어학습 — `/회화` 시작 시 오늘의 주제(순환+관심사 매칭)를 정하고 그 핵심단어를 기존 단어카드 플로우로 먼저 학습시킨 뒤 회화 시작, 프롬프트에 주제/단어 주입, `conversation_sessions.topic/preview_word_ids`로 기록 |
| M20 | 실사용 버그리포트 6건 — (1)신규단어 개수 오인 버그(복습/신규 표시 분리) (2)문법 순차진행이 배치레벨을 건너뛰던 버그(전역 단일 커리큘럼으로 통합) (3)`/학습중단` 명령어+상시버튼 (4)단어암기 효율(능동적상기/연상법/예문다양화/세션요약) (5)문법설명 프롬프트를 쉬운 말로 개선 (6)선정이유 디버그 로깅 |

부가로: 메인 메뉴(리플라이 키보드) + 영문 명령어 `/` 자동완성(한글 명령어는 텔레그램 규칙상 자동완성 불가, 타이핑은 계속 됨), 답변한 인라인 카드는 자동 삭제. 라우터가 이번에 처음으로 텍스트 메시지 외에 **음성메시지(voice)** 도 처리하기 시작함(Stage6 말하기 연습). 단어 카드에 스포일러(모자이크) 답 공개 기능도 추가됨(M18 작업 도중 사용자가 발견한 UX 버그 수정).

## 남은 작업

기획서 섹션17에 명시된 마일스톤(M0~M17) + 추가 요청(M18/M19/M20) 전부 구현 완료. 로드맵상 남은 항목 없음 — 앞으로는 실사용 중 발견되는 버그 수정이나 새로운 개선 요청 위주가 될 것으로 예상.

**M18 관련 후속 참고사항**:
- 그래마 커리큘럼 콘텐츠 생성 중(`scripts/generate_grammar_curriculum.py`) Gemini가 큰 JSON 응답(10문항)에서 가끔 "Extra data" 파싱 에러를 냄 — 스크립트는 이미 실패해도 다음 주제로 넘어가도록 처리되어 있고, `--count-per-topic`을 낮추면(예: 6) 성공률이 올라감. 나중에 콘텐츠를 더 늘리고 싶으면 그냥 다시 실행하면 됨(중복 삽입은 되지만 해롭지 않음).
- 회화 난이도 사후검사는 우리 `words` 테이블(수백 단어 규모)에 있는 단어만 판단 대상이라 재현율 한계가 있음 — 일반 대화에서 쓰이는 모든 단어를 커버하는 진짜 빈도사전은 아님. 필요하면 나중에 외부 빈도 데이터셋 연동을 검토할 것.

**M20 관련 후속 참고사항**:
- `app/grammar/curriculum.py`가 배치레벨별 3목록 → 전역 단일 순서(18개 주제)로 바뀌면서 `users.grammar_topic_index`의 의미가 바뀜 — [migrations/0022](migrations/0022_grammar_curriculum_and_vocab_memory.sql)에서 전원 0으로 리셋했음. **이 마이그레이션을 실제 DB에 아직 안 돌렸다면 반드시 돌릴 것** (위 "배포 서버에 아직 반영 안 됐을 수 있는 것" 참고).
- 문법 concept_intro 프롬프트를 쉬운 말로 개선했지만, 기존에 이미 생성된 `grammar_questions` 행은 옛 스타일 그대로 남아있음 — 새로 원하면 `generate_grammar_curriculum.py`를 다시 돌리되, 중복 삽입 방식이라 옛/새 스타일이 섞여 나올 수 있음(범위 밖으로 판단, 손대지 않음).
- 단어 mnemonic/example_sentences도 마찬가지로 새 콘텐츠부터만 채워짐 — 기존 단어는 `scripts/backfill_word_mnemonics.py`로 소급 백필 가능(선택, 안 해도 무방).

## 알아두면 좋은 사용자 취향/피드백 패턴

- 기능 완성 후 항상 **실제 텔레그램(폴링) 또는 시뮬레이션 스크립트로 실사용 검증**을 원함 — "테스트했다"는 말로 안 끝내고 실제로 돌려봄. 사용자가 직접 클릭하기 번거로우면(문항 많을 때 등) 자동 시뮬레이션 스크립트로 대신함.
- 유닛테스트(모킹)만으로는 못 잡는 버그가 실사용 검증에서 여러 번 나옴(jsonb 타입 불일치, Windows 이벤트루프, Supabase IPv6 등) — 실제 연동 검증을 생략하지 말 것.
- 기획서의 `[결정 필요]` 항목은 막히지 않고 합리적 기본값으로 진행 후 `IMPLEMENTATION_STATUS.md`에 근거와 함께 기록하는 방식을 계속 유지 중.
- 사용자는 "다음 진행해줘"로 다음 마일스톤을 계속 요청하는 스타일 — 마일스톤 끝날 때마다 뭘 했는지 짧게 보고하고 다음으로 넘어가면 됨.
- **"계속 진행할까요?" 같은 확인 질문을 하지 말 것** — 사용자가 명시적으로 요청함(2026-09-07). 마일스톤 사이에 허락을 구하지 말고 바로 다음 마일스톤을 자동으로 진행하고, 테스트까지 마친 뒤 결과만 보고할 것. 단, 이번 M14처럼 **원 기획서에 스펙이 아예 없어서 잘못 짚으면 크게 재작업해야 하는 굵직한 설계 결정**은 예외 — 그때는 진행을 멈추고 사용자에게 직접 확인함(작은 `[결정 필요]` 항목들은 계속 합리적 기본값으로 진행).
