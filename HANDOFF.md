# 세션 인수인계 문서

새 대화(새 탭)에서 이어서 작업할 때 이 문서를 먼저 읽으면 바로 맥락을 파악할 수 있다.
**상세 내역은 [IMPLEMENTATION_STATUS.md](IMPLEMENTATION_STATUS.md)(마일스톤별 완료 내역/근거/버그/결정사항)와 [README.md](README.md)(설치/실행/명령어 목록)에 이미 다 있음 — 이 문서는 그 둘의 요약 + 지금 당장 알아야 할 것만 모은 진입점이다.**

## 한 줄 요약

기획서(가족용 텔레그램 영어학습 봇, 관리자 승인제) 기준 **M0~M12 전부 구현+실제 텔레그램/Gemini/Supabase로 검증 완료**, pytest 60건 통과. 다음은 M13(학교 시험 관리 확장)부터.

## 지금 당장 알아야 할 것 3가지

1. **Gemini 모델은 `gemini-3.1-flash-lite`를 쓴다** (`app/ai/gemini_client.py`). 처음엔 `gemini-3.6-flash`를 썼다가 무료 티어 일일 한도가 프로젝트당 **20회**밖에 안 된다는 걸 실측으로 발견함(최신 모델이라 아직 표준 무료 티어를 못 받음). `gemini-3.1-flash-lite`는 표준 무료 티어(하루 1,500회)에 포함되어 있고, 실제 문법문제 생성 프롬프트로 품질 비교해봐도 손색없어서 이걸로 교체함. **무료 티어 한도는 모델별로 독립적으로 관리된다** — 나중에 더 최신 모델로 바꾸고 싶으면 먼저 직접 호출해서 한도부터 확인할 것.
2. **로컬 실행 시 Windows 전용 이슈 2개**: (1) `uvicorn` 대신 `python run_local.py` 사용(psycopg 비동기가 기본 이벤트루프를 지원 안 함), (2) DB는 Supabase Direct Connection(IPv6 전용, 로컬에서 접속 안 됨) 대신 **Session Pooler** 문자열을 써야 함(`.env`에 이미 반영됨). 배포 환경(Linux/Railway)에는 이 이슈 자체가 없음.
3. **실제 계정 이미 다 세팅되어 있음**: Telegram Bot Token(`@KR_My_Englishbot`), 관리자 텔레그램 ID, Supabase 프로젝트, Gemini API Key 전부 `.env`에 있음(이 대화에서 사용자가 직접 발급해서 줌). 값 자체는 여기 다시 안 적음 — `.env` 파일 참고.

## 로컬에서 바로 이어서 작업하는 법

```bash
python run_local.py              # 서버 실행 (Windows)
python scripts/poll_dev.py       # ngrok 없이 실제 텔레그램과 대화 테스트 (long polling)
pytest                           # 유닛테스트 60건
```

특정 기능을 수동 클릭 없이 끝까지 자동으로 검증하고 싶으면 `scripts/simulate_*.py` 스크립트들을 참고(오늘의학습/해석/회화/텍스트학습 각각 있음) — 실제 DB+Gemini를 타되 텔레그램 발송만 캡처해서 자동 진행.

## 구현 완료 (M0~M12) 한눈에

| 마일스톤 | 핵심 내용 |
|---|---|
| M0~M2 | 프로젝트 뼈대, 관리자 승인, 나이→모드(GENERAL/CHILD_BRIDGE/CHILD_BEGINNER) 배정 |
| M3 | 레벨진단(V0), 인라인 키보드+콜백쿼리 구조 확립(이후 전부 재사용) |
| M4 | 단어/문법/해석 콘텐츠뱅크 AI 대량생성 스크립트 |
| M5 | 단어학습(카드→객관식→주관식) + SRS + 단어시험 |
| M6 | 문법학습(개념설명→객관식) + 오답기록, `/복습` |
| M7 | 오늘의학습(단어→문법→해석→회화 자동 체이닝) + Planner AI 포커스 메시지 |
| M8 | 해석(Reading) — 정답 안 주고 힌트→재시도→공개 |
| M9 | 회화(Conversation) — AI와 5턴 대화, 하루 1회 |
| M10 | `/진도`(개인 통계), 관리자 `/사용자목록`(아이디/나이/모드/오늘활동), 관리자 하루제한 우회 |
| M11 | `/텍스트학습` — 텍스트 붙여넣기 → 핵심단어 카드학습 → 전체해석 → AI피드백 |
| M12 | `/수행평가` — 학교 수행평가 지문 붙여넣기 → 핵심단어 카드학습 → 핵심문법 해설 → 예상 시험문제 3문항(객관식) |

부가로: 메인 메뉴(리플라이 키보드) + 영문 명령어 `/` 자동완성(한글 명령어는 텔레그램 규칙상 자동완성 불가, 타이핑은 계속 됨), 답변한 인라인 카드는 자동 삭제.

## 남은 작업 (기획서 로드맵 기준)

- **M13** 학교 시험 관리 확장 — 시험/단원/자료/선생님강조/D-Day/시험직전복습
- **M14** CHILD_BRIDGE 모드
- **M15** CHILD_BEGINNER 모드(Stage0~6: 알파벳/파닉스/기초단어/기초문장/QA/짧은지문/듣기말하기) — **⚠️ 사용자가 명시적으로 요청**: 이 모드는 학습 내용 자체가 다르므로(성인용 GENERAL 모드와 공유 X) **메인 메뉴/버튼도 완전히 다르게 새로 만들어야 함**. 지금 만든 `app/menu.py`의 GENERAL용 메뉴를 그대로 쓰면 안 됨.
- **M16** TTS 캐싱
- **M17** AI 사용량 모니터링 — `learning_sessions.ai_call_count`로 세션별 집계 기반은 이미 마련됨(Planner에서만 실제로 증가시키는 중, 다른 기능들도 채워 넣을 것)

## 알아두면 좋은 사용자 취향/피드백 패턴

- 기능 완성 후 항상 **실제 텔레그램(폴링) 또는 시뮬레이션 스크립트로 실사용 검증**을 원함 — "테스트했다"는 말로 안 끝내고 실제로 돌려봄. 사용자가 직접 클릭하기 번거로우면(문항 많을 때 등) 자동 시뮬레이션 스크립트로 대신함.
- 유닛테스트(모킹)만으로는 못 잡는 버그가 실사용 검증에서 여러 번 나옴(jsonb 타입 불일치, Windows 이벤트루프, Supabase IPv6 등) — 실제 연동 검증을 생략하지 말 것.
- 기획서의 `[결정 필요]` 항목은 막히지 않고 합리적 기본값으로 진행 후 `IMPLEMENTATION_STATUS.md`에 근거와 함께 기록하는 방식을 계속 유지 중.
- 사용자는 "다음 진행해줘"로 다음 마일스톤을 계속 요청하는 스타일 — 마일스톤 끝날 때마다 뭘 했는지 짧게 보고하고 다음으로 넘어가면 됨.
